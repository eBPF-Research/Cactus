
net_inf: eth0
mode: 0
# use_op: 1
alpha: 0.02


# -------------------
# inf_index: 1
# mode 0: use all ops
# mode 1: use one op (use_op)
#   use_op = 1..4

# 目前 alpha 直接就是触发概率
# alpha * 100 = trigger rate